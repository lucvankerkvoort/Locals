// This page will hold the different API routes we will need for our application
// that we will build into seperate documents and make available through module exporting
module.exports = {
  locals: require("./locals"),
  travellers: require("./travellers")
};
