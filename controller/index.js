// this requires both the controllers so whenever we require this folder we can use its content

module.exports = {
  localController: require("./localController"),
  travellerController: require("./travellerController")
};
